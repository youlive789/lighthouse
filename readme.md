# Google Lighthouse Docker file
by. gwkim

---

### How to use
    - git clone https://github.com/youlive789/lighthouse.git
    - cd lighthouse
    - docker build -t lighthouse .
    - docker run --rm lighthouse <URL> >> result.json